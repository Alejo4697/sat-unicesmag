PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# PostgreSQL - usado por la feature `administracion` (server/src/shared/db/pool.js).
# El resto de features sigue con MOCK_DATA por ahora.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=DataBasePractica
DB_USER=postgres
DB_PASSWORD=123456
